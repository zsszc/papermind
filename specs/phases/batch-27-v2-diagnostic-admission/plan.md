# Batch 27 实施计划

1. RED：冻结历史 dirty 报告默认拒绝、显式准入和 provenance 输出契约。
2. GREEN：实现祖先提交/完整快照校验和 selection-only 标记。
3. RUN：对既有 v2 train 报告生成一次去标识化聚合。
4. DECIDE：按主导失败创建唯一候选规格；正式 train 仍要求 fresh clean 配对报告。
5. VERIFY：全量/公开 Gate、测试报告、台账、提交与 push。
