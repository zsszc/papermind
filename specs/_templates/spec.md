# <模块/功能名称> 规格说明书

> 模板使用说明：本文件描述**行为契约**（做什么），不描述实现细节（怎么做归 plan.md）。
> 每节都要写；没有的写「无」。全部用中文。写完后删除本说明。

## 1. 背景与目标

<!-- 这个模块/功能解决什么问题？为什么存在？一两段话说清。 -->

## 2. 范围

### 2.1 包含

<!-- 本规格覆盖哪些行为 -->

### 2.2 非目标

<!-- 明确不做什么，防止范围蔓延 -->

## 3. 行为契约

<!-- 对每个公开接口/行为，给出输入输出契约。函数签名照抄代码。 -->

### 3.1 `<函数/方法签名>`

- **输入**：<参数、类型、约束>
- **输出**：<返回类型、语义>
- **前置条件**：<调用前必须满足什么>
- **后置条件**：<调用后保证什么>
- **副作用**：<DB 写入 / 文件 I/O / 网络调用 / 全局状态变更，逐一列出>
- **异常**：<什么情况下抛什么错>

### 3.2 `<下一个接口>`

…

## 4. 边界条件与错误处理

| 场景 | 期望行为 |
|------|----------|
| <空输入/超长输入/并发/资源缺失等> | <行为> |

## 5. 依赖

- **上游依赖**：<本模块依赖哪些模块/库/配置项>
- **下游消费者**：<谁调用本模块>

## 6. 验收标准（可测试）

<!-- 每条必须能映射到至少一个自动化测试 -->

- [ ] AC1：<可测试的验收条件>
- [ ] AC2：…

## 7. 现有测试覆盖与盲区

- **已覆盖**：<列出 tests/ 中对应的测试文件与用例>
- **盲区**：<未覆盖的行为/边界，标注严重程度 高/中/低>

## 8. 关键设计决策

<!-- 为什么这样设计？记录取舍与历史原因，防止后人误改 -->

- <决策1>：<理由>
