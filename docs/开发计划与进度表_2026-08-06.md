# PaperMind 新开发计划与进度表（2026-08-06）

## 1. 目标与执行原则

本计划承接 Phase A–H 与 Batch 7–11 的既有逻辑。项目当前已经具备完整业务功能，后续主线从“继续堆功能”切换为“先建立可信 Harness，再消除发布阻断，最后以稳定基准提升 RAG 与 UI”。

所有批次严格执行 `Harness 基线 → SDD spec/plan/tasks → TDD RED/GREEN/REFACTOR → 全量回归 → 测试报告 → 中文 Conventional Commit → push`。没有稳定基准时，RAG 变化只记为实验结果，不直接修改生产默认策略。

## 2. 开发计划表

| 批次 | 优先级 | 目标 | 主要交付物 | 硬 Gate | 状态 |
|---|---|---|---|---|---|
| Batch 12 | P0 | 评测数学正确性与基线可追溯 | citation P/R/F1、摘要引用解析、NDCG 去重、数据/语料指纹、运行降级诊断、测试报告 | 定向 RED/GREEN；后端全绿；离线 eval 报告可复现 | **已完成** |
| Batch 13 | P0 | 公开可复现 RAG fixture 与稳定 qrels | OA/合成语料、证据跨度 qrels、clean-checkout seed→index→eval | 正例全部解析；同指纹两次结果一致；CI 不依赖私人数据 | **下一批** |
| Batch 14 | P0 | 前端/Electron Harness | Vitest + RTL + MSW；Electron `node:test` 与启动/退出 smoke | test/lint/build/audit；关键状态与进程生命周期覆盖 | 待开始 |
| Batch 15–16 | P0 | Electron 发布安全 | Electron/builder 升级、CSP、导航/权限拦截、随机端口与实例令牌 | 两端 audit=0；假服务/无令牌拒绝；制品无密钥和个人数据 | 待开始 |
| Batch 17 | P0 | 数据自愈与上传安全 | dry-run/backup/repair；PDF/DOCX 魔数及资源上限 | 复制库 `foreign_key_check=0`；伪装/zip bomb 用例通过 | 待开始 |
| Batch 18–19 | P1 | 前端可靠性 | Chat 状态/abort/幂等、笔记 flush、WritingDesk body、长请求策略 | 自动化交互全绿；无重复消息、丢写或正文进入 URL | 待开始 |
| Batch 20–22 | P1 | 统一检索与 RAG 提升 | 统一 RetrievalPipeline；中文/数值检索、BM25、query expansion、Graph 消融 | 每批单变量；Recall/MRR/NDCG 不回退；默认 P95 < 1s | 待开始 |
| Batch 23 | P1 | 生成与 Guardrail 闭环 | 生产链路生成评测、引用忠实度、负例拒答、流式安全 | citation P/R/F1；拒答率 ≥ 0.90；越界引用=0 | 待开始 |
| Batch 24+ | P2 | Phase H UI 逐页迁移与发布候选 | 视觉/契约/E2E 基线，逐页替换，安装升级回滚验证 | 关键流程、可访问性、包体预算和桌面 E2E 全绿 | 待开始 |

## 3. RAG 指标阶段目标

| 阶段 | Recall@5 | MRR | NDCG@5 | 分类下限 | 延迟 |
|---|---:|---:|---:|---|---|
| 观察基线（当前动态 qrels） | 0.193 | 0.307 | 0.195 | factoid/summary = 0 | P95 149ms |
| 稳定基准建立后首个门禁 | 不低于冻结值 | 不低于冻结值 | 不低于冻结值 | 任一类型不得回退 > 0.05 | warm P95 < 1s |
| RAG 阶段完成目标 | ≥ 0.60 | ≥ 0.50 | ≥ 0.50 | factoid/summary Recall ≥ 0.40 | warm P95 < 1s |

当前 25 条 QA 集中于单篇示例论文，且相关 chunk 会随分块变化，因此 0.193 仅是观察基线，不作为稳定质量承诺。Batch 12 修数学与追踪，Batch 13 冻结可复现基准，Batch 20 起才允许以稳定集和 holdout 决定默认策略。

## 4. 变更与证据台账

| 日期 | 批次 | 变更 | RED 证据 | GREEN / 回归证据 | 提交 |
|---|---|---|---|---|---|
| 2026-08-06 | Batch 12 | 评测数学、报告 v2、BM25 观察实验 | 3 组 RED，详见测试报告 | 491 passed；Recall@5 观察值 0.198 → 0.439 | `8fe4786`、`441cc1d`、`3ca8504` |

每完成一个微循环都更新本表与 `docs/test-reports/` 对应报告；报告必须记录命令、环境、通过数、指标前后值、已知限制和 Git 提交。
